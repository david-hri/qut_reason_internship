eligible(user, pto)
eligible(user, base_rate_pay)
eligible(user, hourly_rate_pay)
eligible(user, pto_accrual_64)
eligible(user, pto_accrual_120)
eligible(user, pto_accrual_160)
eligible(user, pto_accrual_200)
may_have(user, vacation_denied)
eligible(user, pto_carry_over_40)
requires(user, pto_request_submission)
requires(user, physician_excuse)
eligible(user, accrued_pto)
forfeits(user, accrued_pto)
forfeits(user, accrued_pto)
ineligible(user, pto_donation)
ineligible(user, non_paid_time_off)
eligible(user, holiday_pay)
ineligible(user, holiday_pay)
eligible(user, bereavement_leave)
eligible(user, unpaid_time_off)
